spring:
  main:
    banner_mode: off

  datasource:
    url: jdbc:mysql://localhost:3306/movie_land
    username: hbstudent
    password: hbstudent

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect

logging:
  level:
    org:
      hibernate:
        SQL: debug

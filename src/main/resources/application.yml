spring:
  main:
    banner_mode: off

  datasource:
    url: jdbc:mysql://localhost:3306/movieLand
    username: hbstudent
    password: hbstudent
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect

#  data:
#    rest:
#      base-path: /api
#management:
#  endpoints:
#    web:
#      exposure:
#        include: *

logging:
  level:
    org:
      hibernate:
        SQL: debug


